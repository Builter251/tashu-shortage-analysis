# 동료평가 대응표 — 평가 항목별 근거 위치와 답변

평가 항목을 하나씩 놓고 **① 어디에 있는가 ② 30초로 어떻게 말하는가** 를 정리했다.
리허설은 이 문서만 보면 된다. 수치는 전부 `src/verify_report.py` 로 자동 대조된다(32건 전건 일치).

---

## 항목 1 — 결과물 요건 (체크리스트)

| 요건 | 충족 | 근거 |
|---|---|---|
| 데이터 포인트 100개 이상 | ✅ **9,116,471행** (통행) · 일별 시계열 572일 · 대여소×시간 패널 618만 행 | REPORT 3장 |
| 분석 질문 3개 이상 | ✅ **5개** | REPORT 2장 |
| 시계열 기법 2가지 이상 | ✅ **5가지** (이동평균 / 전년동월 대비 변화율 / 요일×시간 집계 / 누적합 / STL) | REPORT 5-2 |
| 시각화 2개 이상 | ✅ **11개** (링크 전부 정상) | REPORT 6장 · `images/` |
| 시각화 3개 이상 (권장) | ✅ 11개 | 〃 |
| 인사이트 3개 이상 + 관찰 근거 | ✅ **6개 + 빗나간 가설 1개**, 전부 관찰/해석/행동 3층 | REPORT 7장 |
| GitHub 저장소 (코드·리포트·실행/출처) | ✅ 코드 11개 · 노트북 1개 · 리포트 6개 · README 실행 순서 · 출처·라이선스 | 저장소 전체 |

**보너스도 둘 다 했다**
- 보너스 1(서비스화): **배포 URL** https://builter251.github.io/tashu-shortage-analysis/ — 기간·자치구·유형·최소 이용량 필터
- 보너스 2(심화): **(A) STL 분해**(그림 07) + **(B) 베이스라인 예측**(그림 08) 둘 다

---

## 항목 2 — 흐름과 판단 기준

### Q. 데이터 로딩/정제/분석/시각화 흐름을 단계적으로 설명해 보세요

> **한 줄**: 원본 CSV 20개(2.3GB) → 정제 parquet → 대여소×시간 패널 → 지표·그림 → 검증.
> "한 번 무겁게, 이후는 가볍게"가 원칙입니다.

```
01_ingest      인코딩 자동 판별 · 청크 스트리밍 · 정제 플래그   932만 행 / 77초
02_build_panel 전역 중복 제거 · 궤적 재구성 · station_type      911만 행 / 23초
03_analyze     시계열 지표 · 그림 01~07
04_forecast    재고 소진 진단 · 예측 백테스트 · 그림 08~10
07_robustness  반례·민감도 · 그림 11
verify_*       재배치 로직 교차 검증 · 리포트 수치 32건 자동 대조
```

**보여줄 것**: `notebooks/analysis.ipynb` — 각 단계의 판단 근거를 실행 결과와 함께 담았다.

### Q. 결측치/이상치 처리 기준을 "왜 그렇게 정했는지" 설명해 보세요

> **먼저 짚을 것**: `isna().sum()` 은 전부 **0**이었습니다. 그래서 "결측치 없음"으로 끝낼 뻔했는데,
> 실제로는 **의미상 이상치**가 6.5% 있었습니다.

| 규칙 | 왜 이 기준인가 |
|---|---|
| 관제센터 행(대여 2.89% / 반납 3.09%) 분리 | 배포처 안내에 "사용자 실수·반납 오류로 관리자가 강제 처리"라고 명시. 물리적 대여소가 아니다 |
| 대여 취소 = 시간 ≤1분 **AND** 거리 0km (3.56%) | **둘 중 하나만으로는 부족하다.** 1분짜리 짧은 이동도, 제자리 왕복도 실제 이용일 수 있다 |
| 이용시간 > 24시간 (8건) / 거리 > 40km (154건) | 중앙값 11분·1.0km인 서비스에서 다른 성격의 사건. 삭제하지 않고 플래그로 남겨 민감도 확인에만 사용 |
| 결측 37일 보간 안 함 | 보간하면 없는 데이터를 근거로 말하게 된다. 그래프에서 선을 끊었다 |

> **원칙 한 줄**: 어떤 행도 조용히 지우지 않습니다. 전역 중복만 제거하고 나머지는 플래그로 남겨,
> 어떤 규칙이 어떤 결론에 영향을 주는지 언제든 되짚을 수 있게 했습니다.

### Q. 집계 단위(일/주/월)와 그 선택 이유는?

> **전체 추세는 일 단위, 부족 진단은 시간 단위**로 봤습니다.
> 그림 04가 그 이유입니다 — 목동 한사랑아파트가 **06시부터 23시까지 하루 종일 새는** 모양은
> 시간 단위에서만 보입니다. 일 단위로 올리면 "하루 −3.00대" 숫자 하나가 되고
> **"언제 비는가"가 통째로 사라집니다.** 반대로 전체 추세를 시간 단위로 보면 노이즈에 묻혀
> 계절성이 안 보입니다. **질문마다 맞는 집계 단위가 다릅니다.**
> 월별 비교는 결측일 수가 달라서 총합 대신 **일평균으로 정규화**했습니다.

---

## 항목 3 — 시계열 이해와 AI 검증

### Q. 적용한 기법이 "무엇을 보려는 것인지" 설명해 보세요

| 기법 | 무엇을 보려고 |
|---|---|
| **이동평균 7일** | 요일 주기가 7일이므로 **창을 7의 배수로 잡아야** 요일 성분이 정확히 상쇄된다. 5일이나 10일이면 요일 효과가 덜 지워져 곡선이 울퉁불퉁해진다 |
| **전년 동월 대비 변화율** | 절대량은 계절에 좌우된다. 계절을 통제해야 성장 여부가 보인다 |
| **요일 × 시간대 집계** | 부족은 하루 중 몇 시간의 문제다. 일 단위에서는 평균에 묻힌다 |
| **누적합** | 순유출의 누적이 곧 상대 재고 곡선이다. 차분의 역연산 |
| **STL 분해** | 추세·주간 계절성·잔차 분리. 잔차 급락일 = 외생 충격 후보 |

### Q. 트렌드/계절성/노이즈를 그래프에서 어떻게 구분했나요

> **계절성**으로 답하겠습니다. 이 데이터는 계절성이 **세 겹**입니다.
> ① 연 단위 — 1월 6,144건/일 ↔ 9월 19,895건/일, **3.2배** (그림 01)
> ② 주 단위 — 평일 아침 봉우리가 주말에 사라짐 (그림 02)
> ③ 일 단위 — 평일 8시(5.87%)·18시(9.41%) 이중 피크
>
> **어떻게 구분했나**: 7일 이동평균으로 요일 성분을 지우니 연 단위 곡선이 남았고(트렌드+연계절성),
> STL로 주기 7을 분리하니 주간 성분과 잔차가 갈렸습니다. 잔차가 크게 음수인 날
> (2025-06-20, 06-21, 07-17)이 **노이즈**, 즉 기상 같은 외생 충격 후보입니다.
> 다만 기상 데이터로 확인하지 못했으므로 **가설**입니다.

### Q. AI가 만든 코드/해석을 어떻게 검증했나요

> **AI가 만든 숫자는 숫자가 아니다** 를 원칙으로 잡고 네 가지로 검증했습니다.

| 검증 | 방법 | 결과 |
|---|---|---|
| **독립 재구현** | 재배치 탐지 DuckDB 윈도우 함수를 **pandas로 다시 짜서** 대조 | 자전거 300대·재배치 4,034건 **건별 완전 일치** |
| **보존 법칙** | 대여 합계 = 반납 합계, 재배치 유입 = 유출, net_flow 총합 = 0 | 전부 OK. *정답을 몰라도 틀렸다는 것은 알 수 있다* |
| **외부 대칭** | 알고리즘은 순유출을 전혀 안 봤는데 재배치 방향이 맞는가 | 순유출 1위엔 유입 1,216 vs 유출 56, 순유입 1위엔 유출 1,524 vs 유입 173 |
| **수치 자동 대조** | 리포트 인용 수치를 산출물에서 재계산 (`verify_report.py`) | **32건 전건 일치** |

> 그리고 **AI 결과를 그대로 쓰지 않고 되돌린 사례가 실제로 있습니다.**
> 재고 소진 지표는 그럴듯한 값(13.45%)이 나왔지만, 포아송 이론값을 **손으로 계산**해 보니 13.53% —
> 우연과 구분이 안 됐습니다. **지표를 폐기하고 재설계**했습니다.

---

## 항목 4 — 해석·반례·한계 (배점이 큰 부분)

### Q. 인사이트 하나를 관찰 → 원인 → 행동으로 설명해 보세요

> **인사이트 3 — 부족은 총량이 아니라 비대칭의 문제다**
>
> **관찰(Fact)**: 대여량과 일평균 순유출의 상관이 **−0.172**로 사실상 무관합니다.
> 대여량 1위 한밭수목원(63,136건)은 순유출 1.03대/일, 5위 무역전시관은 **0.03대/일**로 거의 완벽한 균형입니다.
> 반면 순유출 1위 목동 한사랑아파트는 대여량 11,697건으로 중위권인데 **하루 2.76대**가 빠집니다. (그림 03)
>
> **원인(Why, 가설)**: 사람이 몰리는 곳은 왕복 이용이 많아 대여와 반납이 상쇄됩니다.
> 문제는 **한 방향으로만 흐르는 곳**입니다. 아파트 단지처럼 아침에 나가서 돌아오지 않는 곳에서 순유출이 쌓입니다.
> 다만 이용 목적은 데이터에 없으므로 확정할 수 없습니다.
>
> **행동(Action)**: **부족 진단의 KPI를 대여량 랭킹이 아니라 순유출 랭킹으로 바꿔야 합니다.**
> 운영 지표를 '이용량 top N'으로 잡고 있다면 정작 비는 대여소를 놓치고 있을 가능성이 높습니다.

### Q. 결과가 달라질 수 있는 반례를 제시해 보세요

> **네 개 있고, 그중 셋은 직접 계산해 봤습니다.** (REPORT 9장 반례 절, 그림 11, `src/07_robustness.py`)

| 반례 | 무엇이 달라지나 |
|---|---|
| **A. 기간** | 성수기와 비수기로 나누면 순위상관 0.81 / 0.71, Top10 겹침 7·6개. **약 3곳 중 1곳(30.7%)은 계절에 따라 순유출↔순유입 방향이 뒤집힙니다.** 비수기 1위는 목동이 아니라 장동 대덕대학교(3.16)입니다 |
| **B. 집계 단위** | 시간 단위로 보면 하루 안 변동폭이 3.12대인데, 일 단위로 올리면 숫자 하나가 되어 "언제"가 사라집니다 |
| **C. 가정** | 재배치 시각을 공백의 중간점 대신 시작/종료로 바꾸면 피크가 **15시↔18시↔21시**로 움직이고 새벽 비중이 25.5%→5.3%로 5배 차이납니다 |
| **D. 외부 변수** | 날씨 미반영. "비수기 이용 증가"가 행태 변화가 아니라 **그해 겨울이 따뜻했던 것**일 수 있습니다 |

> **그래서 말을 고쳤습니다**: "이 대여소가 부족하다"가 아니라
> **"19개월 평균으로는 이 대여소가 가장 부족하다"** 라고 기간을 붙여 말합니다.
> 재배치의 총량·방향은 어느 가정에서도 안 변하므로 그대로 쓰지만, **시각에 근거한 주장은 하지 않습니다.**

### Q. 한계점과 다음에 수집할 데이터는?

> 한계 8가지를 리포트 9장에 표로 정리했는데, **그중 4개가 실시간 데이터 하나로 해소됩니다.**

| 한계 | 해소 방법 |
|---|---|
| 절대 재고를 모름(상대 재고만) | OpenAPI `parking_count` 로 대여소별 오프셋 확정 |
| 재배치 **시각** 신뢰도 낮음 | 실측 시계열과 대조 |
| 재고 소진을 직접 관측 못 함 | 실제 0대 시간 비율 측정 |
| 관제센터의 정체(장부 보정 vs 실제 정비) | 관제 노드에 자전거가 거치돼 있는지 확인 |
| **날씨 미반영** | 기상청 종관기상관측(ASOS) 대전 지점 결합 — **개선 1순위** |
| 거치대 용량 없음 | ⚠ 이 API로도 해소 안 됨(`parking_count`는 가용 대수) — 별도 확보 필요 |

> 수집 모듈은 **이미 만들어 뒀습니다**(`realtime/`). API 명세를 확인해 엔드포인트·인증·필드를 확정했고,
> 키가 없어도 `--selftest` 로 파싱·저장 경로가 통과합니다. 키가 나오면 `.env` 에 넣는 것만으로 동작합니다.

### Q. AI 없이 핵심 결론을 본인 말로 재구성해 보세요

> **세 문장으로 말하겠습니다.**
>
> 1. 대여 이력에는 재고 컬럼이 없어서, **자전거 한 대 한 대의 이동을 이어 붙여** 대여소별 재고와
>    트럭의 재배치를 역추적했습니다.
> 2. 그 결과 **부족은 이용량이 아니라 방향의 비대칭에서 온다**는 것을 확인했고
>    (대여량↔순유출 상관 −0.172), **재고가 부족하면 실제로 수요가 억제된다**는 것을 보였습니다
>    (직전 재고가 평소보다 5대 부족하면 다음 시간 대여가 기대치의 82%).
> 3. 운영사는 방향은 맞게 재배치하고 있지만 양이 부족합니다 — 충족률 50~73%,
>    도시 전체 하루 98건. 그래서 **순유출 랭킹을 KPI로 삼고, 새벽 선제 + 오후 2차 재배치**를 제안합니다.
>
> **AI가 한 일과 제가 한 일의 경계**: 코드 작성과 문장 다듬기는 AI가 빨랐지만,
> **무엇을 물을지, 어떤 기준선과 대조할지, 결과가 이상할 때 무엇을 의심할지**는 제 판단이었습니다.
> 실제로 AI가 낸 결과를 **네 번 되돌렸습니다** — 재고 소진 지표 폐기, 인과 방향 수정,
> 평가 지표를 MAE에서 RMSE로 교체, 대여소 유형 분류 규칙 재설계.

---

## 부록 — 이 분석에서 실제로 틀렸던 9가지

용어 암기보다 이 목록을 이해하는 게 훨씬 강합니다. 전부 **기준선을 세워 대조했기 때문에** 잡혔습니다.

| # | 무엇을 틀렸나 | 무엇으로 잡았나 |
|---|---|---|
| 1 | "25년12월" 파일이 25년 1월의 복제본인 줄 몰랐음 | 월별 날짜 커버리지 점검 |
| 2 | 3월만 초 단위가 없어 366,879행이 조용히 NaT | 같은 커버리지 점검 |
| 3 | 인코딩을 CP949로 고정 | 실행 중 에러 → 자동 판별 |
| 4 | 파일명 정규식 실패 | 유니코드 NFC/NFD |
| 5 | 같은 코드가 매번 다른 답 (89,915→89,981→89,958) | 연속 실행 대조 → 정렬 tie-breaker |
| 6 | 결측일이 대여 0건으로 위장해 평균 6% 왜곡 | 일별 커버리지 재확인 |
| 7 | 재고 소진 지표가 포아송 노이즈만 셈 (13.45% vs 이론 13.53%) | 이론 확률 손계산 |
| 8 | 재고–대여 검정의 인과 방향이 반대 (1.057로 최고) | 결과 부호가 상식과 반대 |
| 9 | MAE만 보면 "아무것도 안 하는 예측"이 최고 (0.560 < 0.591) | '항상 0' 기준선 |

> 마지막에 하나 더 있습니다. 리포트에 적은 인코딩 개수가 8/12로 틀려 있었는데,
> **노트북에서 재계산하다가** 실제로는 9/11인 것을 발견했습니다. 지금은 `verify_report.py` 가 이것도 대조합니다.

---

*관련: [REPORT.md](REPORT.md) · [GLOSSARY.md](GLOSSARY.md) · [notebooks/analysis.ipynb](notebooks/analysis.ipynb) · [대시보드](https://builter251.github.io/tashu-shortage-analysis/)*
