# 데이터 분석·통계 용어집

> **이 프로젝트(타슈 대여소 부족 분석)의 실제 분석 내용을 예시로 삼은 용어집이다.**
> 정의만 외우면 동료평가에서 막힌다. 각 용어마다 **"이 분석의 어디에서 실제로 쓰였는가"** 와
> **"틀리기 쉬운 지점"** 을 함께 적었다. 실제로 틀렸던 것은 ⚠ 로 표시했다.

**목차**
[A. 데이터 다루기](#a-데이터-다루기-기초) ·
[B. 기술통계](#b-기술통계) ·
[C. 시계열](#c-시계열) ·
[D. 확률분포](#d-확률분포와-기준선) ·
[E. 예측·모델링](#e-예측과-모델링) ·
[F. 인과와 추론](#f-인과와-추론) ·
[G. 이 프로젝트 고유 개념](#g-이-프로젝트-고유-개념) ·
[H. 데이터 엔지니어링](#h-데이터-엔지니어링)

---

## A. 데이터 다루기 기초

### 관측 단위 (unit of observation)
데이터 한 행이 무엇 하나를 나타내는가.

- **이 분석**: 원본 한 행 = **통행 1건**(자전거 한 대가 A에서 빌려져 B에 반납된 사건).
  이걸 `대여소 × 1시간` 단위로 다시 집계해 **패널 데이터**를 만들었다(618만 행).
- **주의**: 관측 단위를 바꾸면 답할 수 있는 질문이 바뀐다. 통행 단위로는 "누가 어디로 갔나"를,
  대여소×시간 단위로는 "언제 비었나"를 물을 수 있다.

### 결측치 (missing value)
값이 비어 있는 것. **비어 있는 것만 결측이 아니다.**

- **이 분석**: NaN은 **0건**이었다. 대신 *의미상 결측*이 있었다 —
  반납 대여소가 관제센터로 기록된 3.09%(실제 반납지 미상), 그리고 **2025년 12월 전체(31일)**.
- ⚠ **틀리기 쉬운 지점**: "결측치 없음"으로 끝내면 안 된다.
  `df.isna().sum()` 이 0이라고 데이터가 깨끗한 게 아니다.

### 이상치 (outlier)
분포에서 크게 벗어난 값. **오류일 수도, 진짜일 수도 있다.**

- **이 분석**: 이용시간 24시간 초과 8건, 이용거리 40km 초과 154건.
  삭제하지 않고 **플래그 컬럼**으로 표시해 민감도 확인에만 썼다.
- **원칙**: 어떤 행도 조용히 지우지 않는다. 지웠다면 몇 건을 왜 지웠는지 표로 남긴다.

### 파생변수 (derived variable / feature)
원본에 없지만 계산해서 만드는 변수.

- **이 분석**: 원본에 **재고 컬럼이 없어서** 만들어야 했던 것들 —
  `순유출 = 대여 − 반납`, `상대 재고 = 순유출의 누적합`, `재배치 = 궤적이 끊긴 지점`.
- **핵심**: 좋은 분석의 절반은 "무엇을 파생시킬 것인가"에서 갈린다.

### 정규화 (normalization)
서로 다른 조건을 비교 가능하게 만드는 것.

- **이 분석**: 월별 대여량을 **일평균**으로 나눴다. 2025년 2월은 26일치, 3월은 28일치밖에 없어서
  총합으로 비교하면 결측일 수 차이가 계절 효과로 둔갑한다.
- **또 다른 예**: 요일×시간대 히트맵도 "요일별 관측 일수"로 나눠 **시간당 평균**으로 만들었다.

### 집계 단위 (granularity)
데이터를 어느 시간 폭으로 묶어 볼 것인가.

- **이 분석**: 전체 추세는 **일 단위**, 부족 진단은 **시간 단위**.
- **왜 중요한가**: 목동 한사랑아파트의 "하루 종일 새는" 모양(그림 04)은 시간 단위에서만 보인다.
  일 단위로 묶으면 하루 −2.76대라는 숫자 하나로 뭉개져 **언제** 비는지가 사라진다.

### 기준선 (baseline)
"아무 일도 없다면 얼마가 나와야 하는가". **분석에서 가장 저평가된 도구.**

- **이 분석에서 기준선이 잡아낸 것 3가지**
  1. 재고 소진 지표가 **포아송 노이즈만 세고 있었다** (이론 13.53% vs 산출 13.45%)
  2. 예측 모델이 **'항상 0' 예측보다 MAE가 나빴다** (0.591 vs 0.560)
  3. 재고-대여 검정의 **인과 방향이 반대였다** (최저 재고 구간 비율이 1.057로 최고)
- **습관**: 그럴듯한 숫자가 나오면 **"우연이라면 얼마가 나와야 하나"** 를 먼저 계산한다.

---

## B. 기술통계

### 평균 vs 중앙값
평균은 극단값에 끌려가고, 중앙값은 버틴다.

- **이 분석**: 이용시간 **중앙값 11분, 평균 18.6분**. 최대 1,146분(19시간) 같은 꼬리가 평균을 끌어올린다.
  → 타슈는 "평균 19분 타는 서비스"가 아니라 **"보통 11분 타는 서비스"** 다.
- **판단 기준**: 분포가 한쪽으로 치우쳐 있으면(왜도가 크면) 중앙값을 대표값으로 쓴다.

### 분위수 (quantile) / 십분위 (decile)
데이터를 크기순으로 줄 세워 n등분한 경계.

- **이 분석**: 상대 재고 편차를 **십분위**로 나눠 각 구간의 `실제 대여 ÷ 기대 대여`를 봤다(그림 10).
  1분위(재고 −5.0대) 0.82 → 10분위(+8.4대) 1.36의 단조 기울기.
- **왜 분위수인가**: 절대값으로 구간을 나누면 대여소마다 규모가 달라 비교가 안 된다.
  **대여소별로** 십분위를 매기면 규모 차이가 자동으로 흡수된다.

### 표준편차 (standard deviation) / 변동성
값이 평균에서 얼마나 흩어져 있는가.

- **이 분석**: 예측 모델의 입력 피처로 `roll_std_24`(직전 24시간 표준편차)를 넣었다.
  "최근 이 대여소가 얼마나 요동쳤는가"가 다음 시간을 예측하는 데 쓰인다.

### 상관계수 (correlation coefficient)
두 변수가 함께 움직이는 정도. −1 ~ +1. **0이라고 관계가 없는 건 아니다**(비선형일 수 있음).

- **이 분석의 핵심 상관 3개**
  | 관계 | 상관 | 뜻 |
  |---|---|---|
  | 대여량 ↔ 순유출 | **−0.172** | 무관. **부족은 총량이 아니라 비대칭의 문제** |
  | CPI(첨두 집중도) ↔ 순유출 | **−0.06** | 무관. 이용 유형으로는 부족을 예측 못 함 |
  | 초과 0건 비율 ↔ 순유출 | +0.03 | 무관. 그래서 그 지표를 폐기했다 |
- ⚠ **주의**: 상관이 0에 가깝다는 것도 **결과**다. 이 분석의 가장 중요한 발견 중 둘이 여기서 나왔다.

### 발산형 vs 순차형 (시각화 색 선택)
크기(magnitude)를 보여줄 땐 한 색조의 명암(순차형), **부호**를 보여줄 땐 두 색+중립(발산형).

- **이 분석**: 시간대×요일 히트맵은 파랑 한 색조(순차형),
  순유출 랭킹·지도는 **빨강↔회색↔파랑**(발산형) — 0을 기준으로 방향이 갈리기 때문이다.

---

## C. 시계열

### 시계열 (time series)
시간 순서로 기록된 관측값. 순서 자체가 정보다.

- **이 분석**: 일별 총 대여량(572일), 대여소×시간 순유출(618만 행).

### 추세 (trend)
장기적인 방향성. 계절과 노이즈를 걷어낸 뒤 남는 흐름.

- **이 분석**: 전년 동월 대비 **+18.5% ~ +92.0%**. 타슈 이용은 뚜렷하게 늘고 있다.
  특히 겨울 저점의 상승 폭(+53~92%)이 여름(+18~22%)보다 크다.

### 계절성 (seasonality)
일정 주기로 반복되는 변동. **여러 겹으로 존재할 수 있다.**

- **이 분석은 3겹**:
  | 주기 | 근거 |
  |---|---|
  | 연 단위 | 1월 6,144건/일 ↔ 9월 19,895건/일 = **3.2배** |
  | 주 단위 | 평일↔주말 패턴 차이 (아침 피크가 주말엔 소멸) |
  | 일 단위 | 평일 아침 8시(5.87%)·저녁 18시(9.41%) 이중 피크 |

### 노이즈 / 잔차 (noise / residual)
추세와 계절성으로 설명되지 않고 남은 부분.

- **이 분석**: STL 분해의 잔차가 크게 음수인 날 = 이용량이 설명 밖으로 급감한 날.
  강수·한파 같은 외생 충격 후보다(그림 07).
- **활용**: 잔차는 버리는 게 아니라 **이상 이벤트 탐지기**로 쓴다.

### 이동평균 (moving average)
최근 k개 값의 평균으로 곡선을 매끄럽게 만드는 것.

- **이 분석**: 7일·30일 이동평균(그림 01).
- ⚠ **왜 하필 7일인가** — 요일 효과의 주기가 7일이기 때문이다.
  **창 크기를 주기의 배수로 잡아야 그 성분이 정확히 상쇄된다.** 5일이나 10일로 잡으면
  요일 효과가 덜 지워져 곡선이 울퉁불퉁해진다. 동료평가 단골 질문.

### 차분 (differencing)
값 대신 "직전 대비 변화량"을 보는 것.

- **이 분석**: 순유출 자체가 이미 재고의 1차 차분이다.
  거꾸로 순유출을 **누적(cumsum)** 하면 재고 곡선이 나온다 — 차분의 역연산.

### 시차 / 지연 (lag)
과거 시점의 값. 예측 모델의 기본 재료.

- **이 분석**: `lag_1`(1시간 전), `lag_24`(하루 전 같은 시각), `lag_168`(일주일 전 같은 요일·시각).
  세 개를 함께 넣는 이유는 자전거 수요가 **"방금 전 상황"과 "지난주 같은 시각"을 동시에 닮기** 때문이다.

### 자기상관 (autocorrelation)
시계열이 자기 자신의 과거 값과 얼마나 닮았는가.

- **이 분석**: 24시간·168시간 지점에서 상관이 높다면 일 주기·주 주기가 존재한다는 증거.
  `lag_24`, `lag_168` 을 피처로 넣은 근거이기도 하다.

### 정상성 (stationarity)
평균·분산이 시간에 따라 변하지 않는 성질. 많은 시계열 모델의 전제.

- **이 분석**: 원본은 **비정상**이다(계절 3.2배, 추세 상승).
  그래서 λ를 고정하지 않고 **도시활동지수로 계절을 나눠 준** 뒤에야 기대값이 의미를 가졌다.

### STL 분해 (Seasonal-Trend decomposition using Loess)
시계열을 추세 + 계절 + 잔차로 나누는 기법.

- **이 분석**: 주기 7로 분해(그림 07). 결측 구간은 분해에만 선형 보간하고 **해석에서는 제외**했다.
- ⚠ **주의**: 보간한 구간을 그대로 해석하면 없는 데이터를 근거로 말하는 셈이 된다.
  그래서 그림에 회색 띠로 표시했다.

### 홀드아웃 (hold-out) / 백테스트 (backtest)
학습에 쓰지 않고 떼어 둔 검증 구간 / 과거 시점에 서서 "그때 이 모델을 돌렸다면"을 재현하는 검증.

- **이 분석**: 마지막 4주(2026-03-04~03-31)를 홀드아웃. 학습 252,000행 / 검증 13,440행.

### 데이터 누수 (data leakage)
예측 시점에 알 수 없었던 정보가 모델에 새어 들어가는 것. **성능이 실제보다 좋게 나온다.**

- ⚠ **시계열에서 가장 흔한 사고**: 무작위 분할(`train_test_split(shuffle=True)`).
  미래 데이터로 과거를 맞히는 꼴이 된다.
- **이 분석**: 반드시 **시간 순서를 지켜** 마지막 구간을 떼었다.

---

## D. 확률분포와 기준선

### 포아송 분포 (Poisson distribution)
정해진 시간 동안 드물게 일어나는 사건의 횟수 분포. 평균 λ 하나로 정해진다.
**0건이 나올 확률 = exp(−λ)**

- **이 분석**: 시간당 평균 λ건 대여되는 대여소에서 우연히 0건이 나올 확률을 계산했다.
  | λ | P(0건) | P(2시간 연속 0) |
  |---|---|---|
  | 1.0 | 0.368 | **0.1353** |
  | 2.0 | 0.135 | 0.0183 |
  | 3.0 | 0.050 | 0.0025 |
- ⚠ **이 계산이 지표 하나를 폐기시켰다**: "수요 있는 슬롯인데 2시간 연속 0건"으로 정의한
  재고 소진율의 중앙값이 **13.45%** 였는데, λ≈1에서 우연히 그렇게 될 확률이 **13.53%** 였다.
  **지표가 재고 소진이 아니라 우연을 세고 있었다.**

### 과산포 (overdispersion)
실제 분산이 포아송이 가정하는 것(분산 = 평균)보다 큰 상태.

- **이 분석**: 포아송 보정과 계절 보정을 다 넣어도 초과 0건 지표는 순유출과 무관했다(+0.03).
  대여 도착이 날씨·행사·군집 도착 때문에 **뭉쳐서** 일어나기 때문이다.
  → **"0건이 많다"만으로는 재고 소진을 증명할 수 없다**는 결론.

### 기대값 (expected value)
"평균적으로 얼마가 나와야 하는가". 관측값을 이것과 비교해야 의미가 생긴다.

- **이 분석의 기대값 모델**:
  `λ_t = 기본기대(대여소, 요일, 시간대) × 도시활동지수(그날)`
  도시활동지수 = 그날 대전 전체 대여 건수 ÷ 전체 평균.
  **이 지수 하나로 계절·요일·날씨·행사가 한꺼번에 흡수된다.**

---

## E. 예측과 모델링

### 베이스라인 (baseline) / 베이스라인 사다리
가장 단순한 예측부터 차례로 쌓아 올리며 "복잡한 모델이 값을 하는지" 확인하는 방법.

- **이 분석의 사다리**
  | 단계 | 모델 | MAE | RMSE |
  |---|---|---|---|
  | B0 | Naive — 168시간 전 값 | 0.822 | 1.633 |
  | B1 | Seasonal Mean — 최근 8주 (요일×시간대) 평균 | 0.630 | 1.229 |
  | B2 | GBM — lag·rolling·순환 인코딩 | 0.591 | 1.160 |
  | — | **항상 0** | **0.560** | 1.237 |
- ⚠ **핵심 교훈**: **B0를 못 이기는 모델은 쓸 이유가 없다.** 그리고 이 분석에서는
  '항상 0'이라는 무지성 기준선이 MAE로 전부 이겼다. 기준선을 안 세웠다면
  "MAE 28% 개선"이라고 자랑스럽게 틀린 보고를 했을 것이다.

### MAE / RMSE
- **MAE(평균절대오차)** = 오차 크기의 단순 평균. 단위가 그대로라 해석이 직관적("평균 0.59대 틀린다").
- **RMSE(제곱근평균제곱오차)** = 큰 오차에 더 큰 벌점.

- **이 분석**: MAE로는 '항상 0'(0.560)이 GBM(0.591)을 이기고, RMSE로는 GBM(1.160)이 이긴다.
  왜냐하면 **MAE를 최소화하는 최적 예측은 조건부 중앙값**인데, 시간당 순유출의 중앙값이
  대부분 0이기 때문이다.
- **행동**: 운영 목적이 "재고가 크게 무너지는 순간을 놓치지 않는 것"이라면 **RMSE로 평가해야 한다.**
  **지표 선택이 결론을 바꾼다.**

### 순환 인코딩 (cyclical encoding)
시각(0~23)이나 요일(0~6)처럼 **끝과 처음이 붙어 있는** 값을 원 위의 좌표로 바꿔 넣는 것.

```
hour_sin = sin(2π × hour / 24),   hour_cos = cos(2π × hour / 24)
dow_sin  = sin(2π × dow  / 7 ),   dow_cos  = cos(2π × dow  / 7 )
```

- **왜**: 그냥 숫자로 넣으면 모델이 "23시와 0시는 23만큼 멀다"고 오해한다. 실제로는 이웃이다.
- **이 분석**: B2 모델의 입력 피처.

### 롤링 피처 (rolling feature)
과거 일정 구간의 요약 통계.

- **이 분석**: `roll_mean_3`(직전 3시간 평균), `roll_std_24`(직전 24시간 변동성).
  단일 시점 값보다 노이즈에 덜 흔들려 추세를 안정적으로 전달한다.

### GBM (Gradient Boosting Machine, 그래디언트 부스팅)
얕은 결정트리를 하나씩 순차로 더하되, **각 트리가 직전까지의 예측이 틀린 만큼(잔차)** 을
맞추도록 학습하는 방식.

- **강점**: 표(tabular) 데이터에서 강하고, "평일이면서 18시이고 직전 1시간 유출이 컸다면" 같은
  조건이 겹치는 비선형 관계를 자동으로 잡는다.
- **이 분석**: `sklearn.ensemble.HistGradientBoostingRegressor` — 추가 설치 없이 결측치도 그대로 받는다.

### 과적합 (overfitting)
학습 데이터의 우연한 패턴까지 외워서, 새 데이터에서 성능이 떨어지는 것.

- **이 분석의 방어**: ① 시간 순서 유지 분할, ② early stopping(검증 15%), ③ 베이스라인 대비 비교.

---

## F. 인과와 추론

### 상관 ≠ 인과
함께 움직인다고 하나가 다른 하나를 일으킨 것은 아니다.

- **이 분석**: 재고가 낮을 때 대여가 적다(그림 10)는 **강한 연관**이지만 실험이 아니다.
  리포트 9장에 "인과 추론이 아니다"를 한계로 명시했다.

### 역인과 (reverse causality)
원인과 결과가 뒤바뀐 것.

- ⚠ **이 분석에서 실제로 밟은 지뢰**: 재고와 대여를 **같은 시각**으로 묶었더니
  최저 재고 구간의 `실제/기대 대여` 비율이 **1.057** 로 오히려 가장 높게 나왔다.
  당연하다 — **많이 빌려 갔기 때문에 재고가 낮아진** 시간이 저재고 구간에 몰린 것이다.
  → **직전 시각의 재고**로 묶자 0.82로 뒤집혔다. **원인이 결과보다 앞서야 한다.**

### 교란변수 (confounder)
두 변수 모두에 영향을 주어 가짜 관계를 만드는 제3의 변수.

- **이 분석**: 계절이 대표적 교란변수였다. 겨울엔 대여도 적고 0건도 많다 →
  보정하지 않으면 "겨울 = 재고 소진"이라는 가짜 결론이 나온다.
  **도시활동지수**로 이를 통제했다.

### 선택 편향 (selection bias)
분석 대상을 고르는 방식 자체가 결과를 왜곡하는 것.

- ⚠ **이 분석에서 실제로 발생**: 조밀 패널을 전 기간(572일)으로 만들었더니
  **대여소가 생기기 전 기간의 0건**까지 세어, 재고 소진 순위 상위를 신설 대여소가 독식했다.
  → 각 대여소의 `first_seen ~ last_seen` 구간으로 제한해 해결.

### 검열 (censoring) / 검열된 수요 (censored demand)
관측값이 어떤 한계에 잘려 있어 진짜 값을 볼 수 없는 상태.

```
관측 대여 = min(실제 수요, 그 시점 재고)
```

- **이 분석의 출발점이자 결론**: 자전거가 0대면 수요가 있어도 **대여 0건**으로 기록된다.
  그래서 "대여가 적다 = 수요가 없다"로 읽으면 정확히 반대로 해석하게 된다.
- **증명**: 직전 재고가 평소보다 5대 부족하면 다음 시간 대여가 기대치의 **82%** 로 떨어진다.
  즉 **관측된 이용량은 실제 수요의 하한**이다.
- **파급**: 예측 모델의 학습 타깃 y 자체가 아래로 잘려 있어 **예측은 구조적으로 과소추정**된다.

### 민감도 분석 (sensitivity analysis)
"이 규칙을 바꾸면 결론이 뒤집히는가"를 확인하는 것.

- **이 분석**: 임시 대여소 포함/제외 두 버전으로 전체 추세를 비교하도록 설계했다.
  결론이 규칙에 민감하면 그 규칙은 결론의 일부로 보고해야 한다.

---

## G. 이 프로젝트 고유 개념

### 순유출 (net outflow)
`대여 − 반납`. 양수면 자전거가 빠져나가는 대여소, 음수면 쌓이는 대여소.

- **이 분석의 핵심 지표**. 순유출 1위 목동 한사랑아파트 **하루 +2.76대**,
  최하위 둔산동 정부청사 남문 **하루 −2.08대**.

### 상대 재고 (relative inventory)
순유출의 누적합. **절대 대수는 모르고 변화만 아는** 재고 곡선.

- **왜 '상대'인가**: 원본에 시작 시점의 거치 대수가 없어 오프셋을 알 수 없다.
  그래서 순위 비교와 "평소 대비 지금 얼마나 낮은가"에만 쓸 수 있다.
- **해소 방법**: OpenAPI 실측으로 대여소별 오프셋을 확정하면 절대값이 된다(6단계 검증 ⑤).

### 드리프트 (drift)
상대 재고가 한 방향으로 계속 흘러가는 현상.

- **이 분석**: 드리프트의 **기울기 = 그 대여소가 유지되기 위해 사람이 매일 채워야 하는 대수**.
  단순한 부산물이 아니라 **운영 부담량**이라는 의미를 갖는다.

### 재배치 (rebalancing)
운영사가 트럭으로 자전거를 옮기는 것. **대여 이력에 직접 기록되지 않는다.**

- **탐지 방법**: 자전거 b의 통행을 시간순으로 이었을 때
  `이전 반납 대여소 ≠ 다음 대여 대여소` 라면 그 사이에 사람이 옮긴 것이다.
- **규모**: 물리적 재배치 56,041건 ÷ 572일 = **하루 98.0건** (자전거 6,776대 규모).
- **검증**: 순유출 상위 대여소엔 재배치 **유입**(ST0051: 유입 1,269 vs 유출 58),
  순유입 상위엔 **유출**(ST1033: 유출 1,529 vs 유입 184)이 잡혔다.
  알고리즘이 순유출을 전혀 참조하지 않았는데도 방향이 맞은 것이 **탐지가 옳다는 외부 증거**다.

### 재고 소진 (stockout)
자전거가 0대라 빌릴 수 없는 상태. **직접 관측이 불가능**해 대리지표로 추정해야 한다.

- **이 분석**: 0건 개수를 세는 접근은 실패했고(과산포), 상대 재고를 직접 쓰는 접근이 성공했다(그림 10).

### CPI (Commute Peak Index, 첨두 집중도)
```
CPI = (평일 07:00~09:59 대여 + 평일 17:00~19:59 대여) / 평일 전체 대여
```
- **기준선**: 이 6시간은 하루 24시간의 정확히 25%. 이용이 고르게 퍼졌으면 **CPI ≈ 0.25**.
- **결과**: 부족 예측에는 쓸모없었다(순유출과 상관 −0.06).
  **유형은 시간 프로파일의 *모양*을 재는데, 부족은 *방향*의 비대칭에서 오기 때문.**

### WR (Weekend Ratio, 주말비)
```
WR = 주말 1일 평균 대여 ÷ 평일 1일 평균 대여
```
- WR < 1 → 주말에 줄어듦(통근성) · WR > 1 → 주말에 늘어남(여가성)
- **예**: 만년동 한밭수목원 **WR 2.19** → 여가형으로 정확히 분류됐다.

### 도시활동지수 (city activity index)
```
도시활동지수(t) = 그날 대전 전체 대여 건수 ÷ 전체 평균
```
- **용도**: 대여소별 기대값에 곱해 계절·날씨·요일·행사를 **한꺼번에** 보정한다.
  기상 데이터를 못 구했을 때 쓸 수 있는 실용적인 대체재.

---

## H. 데이터 엔지니어링

### 인코딩 (encoding)
문자를 바이트로 바꾸는 규칙. 틀리면 한글이 깨지거나 읽기 자체가 실패한다.

- ⚠ **이 분석**: 포털 안내는 "CP949"라고만 했지만 실제로는 **cp949 8개 / utf-8-sig 12개 혼재**였다.
  하나로 고정하면 두 번째 파일에서 `UnicodeDecodeError`.
- **판별 요령**: BOM(`EF BB BF`) 확인 → utf-8 시도 → cp949 시도.
  단, 앞부분만 잘라 읽으면 멀티바이트 문자가 중간에서 끊겨 멀쩡한 파일도 오판한다.
  **증분 디코더**(`codecs.getincrementaldecoder`)를 써야 한다.

### 유니코드 정규화 (NFC / NFD)
같은 한글도 저장 방식이 두 가지다. NFC는 '녀' 한 글자, NFD는 ㄴ+ㅕ 자모 분리.

- ⚠ **이 분석**: macOS 파일명이 NFD여서 `re.search(r"\((\d{2})년(\d{2})월\)", name)` 이 매칭 실패.
  → `unicodedata.normalize("NFC", name)` 필수.

### 청크 처리 (chunking)
큰 파일을 조각으로 나눠 읽어 메모리 초과를 피하는 것.

- **이 분석**: 2.3GB를 `chunksize=400_000` 으로 스트리밍 읽기. 20개 파일 932만 행을 **77초**에 적재.

### Parquet
열 지향(columnar) 압축 저장 형식. 필요한 컬럼만 읽을 수 있어 분석에 빠르다.

- **이 분석**: CSV 2.3GB → Parquet 약 150MB. 이후 모든 분석이 여기서 시작한다.
- **전략**: **"한 번 무겁게, 이후는 가볍게."**

### 윈도우 함수 (window function)
행 사이의 관계(직전 값, 누적합, 순위)를 SQL에서 계산하는 도구.

- **이 분석**: 자전거 궤적 잇기에 `lag(ret_st) OVER (PARTITION BY bike_id ORDER BY rent_dt)`,
  재고 누적에 `sum(net_flow) OVER (PARTITION BY station_id ORDER BY ts)`.

### 동점 처리 (tie-breaker)
정렬 키가 같은 행들의 순서를 확정하는 보조 키.

- ⚠ **이 분석에서 재현성을 무너뜨린 범인**: 같은 스크립트를 세 번 돌렸더니
  재배치 건수가 **89,915 → 89,981 → 89,958** 로 매번 달랐다.
  원인은 2025년 3월 파일에 초 단위가 없어 같은 자전거의 서로 다른 통행이
  **동일한 `rent_dt`** 를 갖는 경우가 377건 있었던 것.
  → `ORDER BY rent_dt, ret_dt, rent_st, ret_st, dur_min` 로 해결.
- **원칙**: **시계열 윈도우 함수의 정렬 키에는 항상 tie-breaker를 넣는다.**

### 재현성 (reproducibility)
같은 코드가 같은 입력에 대해 항상 같은 결과를 내는 것.

- **이 분석의 장치**: 버전 고정 `requirements.txt`, 실행 순서 문서화,
  `verify_02.py`(재배치 로직 교차 검증), `verify_report.py`(리포트 인용 수치 23건 자동 재계산).

### 보존 검증 (conservation check)
물리적으로 반드시 성립해야 하는 항등식으로 파이프라인을 검증하는 것.

- **이 분석**: `대여 총합 = 반납 총합`, `재배치 유입 = 유출`, `전체 net_flow 합 = 0`.
  자전거는 사라지지도 생기지도 않으므로 이게 안 맞으면 어딘가 틀린 것이다.
- **왜 좋은가**: 정답을 몰라도 **틀렸다는 것은 알 수 있다.**

### 교차 검증(구현 대조)
같은 계산을 **다른 코드 경로**로 다시 해서 대조하는 것.

- **이 분석**: DuckDB 윈도우 함수로 만든 재배치 결과를 pandas(`sort_values`+`shift`)로
  독립 재구현해 자전거 300대·재배치 4,034건을 **건별 대조** → 완전 일치.
- **원칙**: AI가 만든 코드는 특히 이렇게 검증한다. **AI가 만든 숫자는 숫자가 아니다.**

---

## 부록. 이 프로젝트에서 실제로 틀렸던 9가지

용어를 외우는 것보다 이 목록을 이해하는 게 동료평가에서 훨씬 강하다.

| # | 무엇을 틀렸나 | 어떤 개념이 잡아냈나 |
|---|---|---|
| 1 | "25년12월" 파일이 25년 1월의 복제본인 줄 몰랐음 | 데이터 커버리지 점검 |
| 2 | 3월만 초 단위가 없어 366,879행이 조용히 NaT | 결측 탐지 · `errors="coerce"` 의 위험 |
| 3 | 인코딩을 CP949로 고정 | 인코딩 자동 판별 |
| 4 | 파일명 정규식 실패 | 유니코드 정규화(NFC/NFD) |
| 5 | 같은 코드가 매번 다른 답 | 재현성 · 동점 처리 |
| 6 | 결측일이 대여 0건으로 위장해 평균 6% 왜곡 | 정규화 · 분모 관리 |
| 7 | 재고 소진 지표가 우연만 셈 | 포아송 기준선 |
| 8 | 대여소 개설 이전 기간까지 집계 | 선택 편향 |
| 9 | 재고-대여 검정의 인과 방향이 반대 | 역인과 |

> 9개 모두 **"기준선을 세워 대조했기 때문에"** 잡혔다.
> 그럴듯한 숫자가 나왔을 때 멈추지 않고
> **"우연이라면 얼마가 나와야 하나"** 를 계산하는 것 — 이 프로젝트에서 배운 가장 중요한 습관이다.

---

*관련 문서: [REPORT.md](REPORT.md) · [PLAN.md](PLAN.md) · [reports/](reports/)*
