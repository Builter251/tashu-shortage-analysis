# M1-1 미션 수행 계획서 — 타슈 대여소 자전거 부족 분석

> 주제: **대전 공영자전거(타슈), 왜 어떤 대여소에는 늘 자전거가 없을까?**
> 데이터: 대전교통공사 타슈 대여이력 정보 (2024-08 ~ 2026-03, 20개월, 약 2.3GB) + 타슈 위치 현황(2026-06-24)
> 이 문서는 "무엇을 만들지"가 아니라 **"어떤 순서로 생각하고, 동료평가에서 어떻게 설명할지"** 를 정리한 설계서다.

---

## 0. 한 줄 요약 (엘리베이터 피치)

> "대여 이력에는 재고(현재 거치 대수) 컬럼이 없습니다. 그래서 **자전거 한 대 한 대의 이동을 이어 붙여 대여소별 재고를 역추적**했고, 그 과정에서 '스스로 균형이 맞지 않아 매일 트럭이 채워줘야만 유지되는 대여소'를 수치로 골라냈습니다. 그리고 관측된 대여 건수는 수요가 아니라 **재고에 의해 잘린 수요(censored demand)** 라는 점을 분석의 출발점으로 삼았습니다."

이 두 문장이 이 미션의 차별점 전부다. 나머지는 이걸 뒷받침하는 작업이다.

---

## 1. 데이터 실사 결과 (**20개 파일 9,320,018행 전수 적재 완료** — `reports/01_ingest_findings.md`)

| 항목 | 확인된 사실 | 분석 설계에 주는 함의 |
|---|---|---|
| 인코딩 | CP949 | `encoding='cp949'` 고정. 아니면 전부 깨짐 |
| 컬럼 | 19개 (자전거번호, 대여/반납 일시·대여소ID·대여소명·좌표·구·동·주소, 이용시간(분), 이용거리(km)) | **재고/거치대수 컬럼 없음** → 재고는 반드시 추정해야 함 |
| 결측치 | 샘플 내 0건 | "결측치 없음"이 아니라 **"빈 값이 아닌 형태로 숨어있다"** 고 봐야 함 (좌표 0, 거리 0, 관제센터 등) |
| 관제센터 행 | 대여 기준 2.78%, 반납 기준 2.93% | 물리적 대여소가 아님. 가상 노드로 분리 처리 |
| 이용시간 | 중앙값 11분, 최대 1146분(19시간), 1분 이하 3.9% | 상한 이상치 + 대여 취소 의심 건 처리 기준 필요 |
| 이용거리 | 중앙값 1.0km, 최대 81.8km, 0km 5.8% | 거리 0 & 시간 ≤1분 = 대여 취소로 간주 후보 |
| 대여소 수 | 1,284개 (1개월 기준) | 20개월 동안 신설/폐쇄 존재 → 대여소 마스터 관리 필요 |
| 자전거 수 | 3,508대 (1개월 기준) | 재고 역추적의 모수 |
| 동일 대여소 반납 | 8.6% | 왕복 이용 or 취소. 순유출 계산에서 자동 상쇄됨(문제 없음) |

**전수 적재 후 확정된 수치** (2026-03 배포본)

| 항목 | 값 |
|---|---|
| 적재 행 수 | 9,320,018 (원본 `wc -l` 합계와 정확히 일치) |
| **유효 행 수** | **9,116,473** (중복 배포된 2025-12 파일 203,545행 제외) |
| **실질 분석 기간** | **19개월** (2024-08 ~ 2026-03, **2025-12 전체 결측**) |
| 대여소 | 1,425개 (관제 가상 노드 2개 포함) |
| 관제센터 관련 | 대여 2.89% / 반납 3.09% |
| 대여 취소 추정(≤1분 & 0km) | 3.56% |
| 24시간 초과 / 40km 초과 / 시간 역전 | 8건 / 154건 / **0건** |
| 좌표 결측 / 범위 이상 | 0건 / 0건 (lat 36.25~36.48, lon 127.29~127.48) |

> 동료평가 포인트: **"결측치 0건이었다"로 끝내지 말 것.** "NaN은 0건이었지만, 관제센터 3%·대여 취소 3.6% 같은 *의미상 이상치*가 있었고, 그보다 더 큰 문제로 **한 달치 파일이 다른 달 데이터의 완전 중복**이었다"가 정답이다.

### 1-1. ★ 원본 데이터에서 실제로 발견한 함정 (전부 직접 잡은 것)

| # | 함정 | 어떻게 잡았나 | 안 잡았으면 |
|---|---|---|---|
| 1 | **"25년12월" 파일 = 2025년 1월 데이터의 완전 중복** (통행키 일치율 100.00%) | 월별 날짜 커버리지 점검 + 두 파일 교차 검증 | 1월 수요가 2배로 잡히고 12월이 있는 것처럼 보임. 겨울 해석 전체가 붕괴 |
| 2 | **2025년 3월만 날짜에 초가 없음** (`2025-03-04 11:02`) | 포맷 고정 파싱 후 3월 366,879행이 전부 NaT → 커버리지 점검에서 발견 | 에러 없이 **한 달이 조용히 사라짐** |
| 3 | **인코딩이 파일마다 다름** (cp949 9개 / utf-8-sig 11개) | 두 번째 파일에서 `UnicodeDecodeError` | 포털 안내대로 CP949 고정 시 적재 자체가 중단 |
| 4 | **파일명 한글이 NFD(자모 분리)** — macOS | 정규식 연월 추출 실패 | 파일 목록 자체를 못 만듦 |
| 5 | **대여소명·주소 표기가 월마다 흔들림** | 같은 `ST0038`의 이름/주소가 월별로 상이 | 이름으로 집계하면 한 대여소가 여러 개로 쪼개짐 → **ID로만 식별** |
| 6 | **`X좌표`가 위도, `Y좌표`가 경도** (통념과 반대) | 값 범위 확인 (36.x vs 127.x) | 지도가 뒤집힘 |
| 7 | **같은 스크립트가 매번 다른 답을 냄** (재배치 89,915 → 89,981 → 89,958) | 연속 실행 결과 대조 | 재현성 붕괴. 원인은 #2(3월 분 해상도)로 인한 정렬 동점 377건 → 윈도우 `ORDER BY` 에 tie-breaker 추가로 해결 |

> 이 표 자체가 "데이터 분석 사고" 평가 항목의 최고 근거다. 남들은 대개 1·2번을 못 잡는다.

---

## 2. 이 분석의 핵심 아이디어 3가지

### 아이디어 A. 대여이력 → 대여소별 재고 시계열 역추적

대여소 s의 재고 변화는 결국 이벤트의 누적합이다.

```
대여 이벤트: 대여소 s 에서 -1 (대여일시)
반납 이벤트: 대여소 s 로 +1 (반납일시)
상대재고(s, t) = Σ(+1/-1)  ...  t 시점까지 누적
```

절대 재고는 알 수 없으므로 **상대 재고(오프셋 미지)** 다. 이 한계는 숨기지 말고 리포트에 명시하고, 아래 두 가지로 보정한다.

1. **하한 앵커링**: 새벽(03~05시) 등 활동이 없는 시간대의 관측 최소값을 0 근처로 가정해 오프셋 추정
2. **실측 앵커링(보너스)**: 타슈 OpenAPI로 수집한 실시간 거치 대수를 정답 라벨로 삼아 추정 재고를 검증

### 아이디어 B. 자전거 궤적의 "끊긴 지점" = 재배치(트럭) 이벤트

자전거 b의 이벤트를 시간순 정렬했을 때:

```
trip j   : ... → 반납 대여소 R_j (시각 t_j)
trip j+1 : 대여 대여소 P_{j+1} (시각 t_{j+1}) → ...

if R_j != P_{j+1}  →  그 사이에 사람이 옮겼다 = 재배치(또는 관제센터 회수/재투입)
```

이게 이 분석의 **가장 강력한 무기**다. 왜냐하면:

- 재배치를 반영하지 않으면 누적 순유출이 한없이 음(-)으로 드리프트한다.
- 그 **드리프트 기울기 자체가 "이 대여소는 하루 평균 N대를 사람이 채워 넣어야만 유지된다"는 운영 부담량**이다.
- 즉, "자전거가 부족한 대여소" = 단순히 대여가 많은 곳이 아니라, **구조적으로 순유출이 나서 외부 개입 없이는 비는 곳**이다.

> 동료평가 포인트: "인기 대여소 = 부족 대여소"가 아니라는 것을 데이터로 보여주면 설명이 확 산다. 대여 1위 대여소가 반납도 1위라면 그곳은 부족하지 않다.

### 아이디어 C. 관측된 대여 건수 ≠ 수요 (검열된 수요, censored demand)

```
관측 대여 = min(실제 수요, 그 시점 재고)
```

자전거가 0대인 대여소는 **대여 건수가 0으로 기록**된다. 그래서 "대여가 적은 대여소 = 수요가 없는 대여소"라고 읽으면 정확히 반대로 해석하게 된다.
사용자 본인의 경험(집 앞에 자전거가 없어서 못 빌림)이 바로 이 **기록되지 않은 수요**다.

> **확인됨(3·4단계)** — 대여량 상위 5개 대여소의 일평균 순유출은 전부 ±1대 이내인 반면,
> 순유출 1위 ST0051(목동 한사랑아파트)은 대여량 중위권인데 하루 **2.76대**씩 빠져나간다.
> 대여량↔순유출 상관 **−0.172**. **부족은 총량이 아니라 비대칭의 문제**다.
>
> **반대로 빗나간 가설도 있다** — CPI/WR 이용 유형은 부족을 예측하지 못했다(상관 −0.06).
> 순유출 상위 30개 중 출퇴근형은 6개뿐이고 23개가 혼합/생활형이다.
> 유형은 *시간 프로파일의 모양*을 재는데, 부족은 *방향의 비대칭*에서 오기 때문이다.
> 이 빗나감은 리포트에 그대로 싣는다 — 인사이트 3을 오히려 강화한다.

**재고 소진 대리지표(stockout proxy)** 정의:
> 해당 대여소의 (요일 × 시간대) 평균 대여 건수가 충분히 높은데(예: 상위 분위), 실제 그 슬롯의 대여가 0건이고 그 상태가 K시간 이상 연속된 구간 = **재고 소진 의심 구간**

이 지표로 "몇 시에, 얼마나 자주, 얼마나 오래 비는가"를 정량화한다.

---

## 3. 분석 질문 (미션 요구: 3개 이상 → 5개로 설정)

| # | 질문 | 유형 | 답하는 방법 |
|---|---|---|---|
| Q1 | 타슈 전체 이용량은 시간에 따라 어떤 추세와 계절성을 갖는가? | 그래프로 확인 | 일별 대여량 시계열 + 7일/30일 이동평균 + 월별 집계 |
| Q2 | 하루 중·요일별 이용 패턴은 어떤 모양인가? 출퇴근형인가 여가형인가? **(유형 정의는 3-1)** | 그래프로 확인 | 시간대 × 요일 히트맵 + 유형 지표(CPI·WR) 산출 |
| Q3 | **구조적으로 자전거가 빠져나가기만 하는 대여소는 어디인가?** | 추가 해석 필요 | 대여소별 순유출(반납−대여) 랭킹 + 누적 드리프트 기울기 |
| Q4 | **부족은 언제, 어떤 패턴으로 발생하는가? (아침/저녁, 평일/주말)** | 추가 해석 필요 | 대여소 × 시간대 순유출 프로파일 + 재고 소진 의심 구간 |
| Q5 | **특정 대여소의 다음 시간대 가용 대수를 예측할 수 있는가?** | 보너스(예측) | 요일×시간 베이스라인 vs lag 기반 모델, 백테스트 MAE 비교 |

> "왜 이런 상황이 발생하는가"(사용자의 원래 질문)는 Q3+Q4의 **해석** 파트에서 다룬다: 지형(고지대→저지대 단방향 이동), 주거지↔업무지 비대칭, 대중교통 환승 지점, 대학가 등. 이건 **가설**이고, 근거로 좌표·행정동·시간대 패턴을 붙인다.

### 3-1. 이용 패턴 유형의 조작적 정의 (Q2 보조)

"출퇴근형/여가형"은 인상이 아니라 **계산 가능한 두 지표**로 정의한다. 정의를 먼저 못 박아야 평가에서 "그건 어떤 기준으로 판단한 건가요?"에 답할 수 있다.

**지표 1 — 첨두 집중도 CPI (Commute Peak Index)**

```
CPI = (평일 07:00~09:59 대여 + 평일 17:00~19:59 대여) / 평일 전체 대여
```
- 이 6시간은 하루 24시간의 정확히 25%다. 따라서 이용이 하루에 고르게 퍼져 있으면 **CPI ≈ 0.25** 가 자연스러운 기준선(null baseline)이다.
- CPI가 0.25보다 뚜렷이 높다 = 출퇴근 첨두에 몰려 있다.

**지표 2 — 주말비 WR (Weekend Ratio)**

```
WR = 주말 1일 평균 대여 건수 / 평일 1일 평균 대여 건수
```
- WR < 1 → 주말에 이용이 줄어든다 (통근성 수요)
- WR > 1 → 주말에 이용이 늘어난다 (여가성 수요)

**분류 규칙** (임계값은 잠정. 전 대여소 분포를 그린 뒤 사분위 기준으로 재보정한다 → **3-1절에서 이행함**)

| 유형 | 조건 | 읽는 법 |
|---|---|---|
| **출퇴근형** | CPI ≥ 0.40 **AND** WR ≤ 0.8 | 평일 첨두에 몰리고 주말엔 빠진다 |
| **여가형** | WR ≥ 1.1 **AND** CPI ≤ 0.30 | 주말·낮 시간대에 완만하게 퍼진다 |
| **혼합/생활형** | 그 외 | 통학·장보기 등 상시 수요 |
| **판정 불가** | 월평균 대여 30건 미만 | 표본이 적어 지표가 요동친다 |

**설계상 주의 (그대로 평가 답변이 된다)**

- 임계값 0.40 / 0.8 / 1.1은 **문헌값이 아니라 내가 정한 선**이다. 리포트에는 CPI·WR 히스토그램을 함께 싣고 "왜 이 선인지" 한 줄을 남긴다. → **아래 3-1절에서 이행.**
- 이용량이 적은 대여소는 두 지표가 심하게 흔들리므로 위 표처럼 **판정 불가 구간을 명시적으로 둔다.** (억지로 분류하지 않는 것도 분석 판단이다.)
- 유형은 **대여 기준과 반납 기준을 따로 계산한다.** 아침에 빠지기만 하는 곳과 아침에 쌓이기만 하는 곳은 둘 다 "출퇴근형"이지만 운영상 정반대 문제다. 이 쌍(pair)이 그대로 Q3(순유출) 해석의 근거가 된다.


### 3-1. 임계값을 어디에 그었고, 왜 거기인가 — 사후 검증

> 설계 시점에 "분포를 보고 재보정한다"고 적어 두었으므로, 그 약속을 이행한 기록을 남긴다.
> 재현: `python3 src/08_threshold_calibration.py` -> `images/12_threshold_calibration.png`, `data/processed/threshold_sensitivity.csv`

**1) 기준선(null baseline)이 있는 값과 없는 값을 구분한다.**

| 지표 | 기준선 | 근거 |
|---|---|---|
| CPI | **0.25** | 첨두로 잡은 6시간 / 하루 24시간. 시간대 분포가 완전히 균등하면 CPI는 정확히 0.25가 된다. 정의에서 나오는 값이므로 논쟁의 여지가 없다. |
| WR | **1.00** | 주말 일평균 / 평일 일평균. 비율이므로 1이 "주말과 평일이 같다"는 구조적 분기점이다. |

WR을 1 기준으로 읽는 판단은 **맞다.** 비율 지표의 1은 임의로 고른 수가 아니라 분모와 분자가 같아지는 지점이다.

**2) 그러나 기준선은 '대여소를 나누는 선'이 되지 못한다.**

관측된 CPI는 **1,243개 판정 대상 중 98.1%가 0.25를 넘는다** (중앙값 0.375). 즉 대전 타슈는 도시 전체가 출퇴근 쪽으로 기울어 있다. 이것 자체가 하나의 발견이지만, 0.25로 자르면 거의 모든 대여소가 "출퇴근형"이 되어 분류가 아무 정보도 주지 못한다. **절대 기준선은 "패턴이 있는가"에는 답하지만 "어느 대여소가 더 그런가"에는 답하지 못한다.** 후자는 상대 기준(분위수)이어야 한다.

**3) 그래서 채택한 선과, 그 선의 실제 위치.**

| 채택선 | 설계 시점의 이유 | 사후 확인된 위치 |
|---|---|---|
| CPI >= 0.40 | 0.25의 약 1.6배 — "기준선보다 뚜렷이 높다"고 부를 만한 크기라고 판단 | 상위 사분위 **q75 = 0.415** 와 사실상 같은 자리 (해당 33.2%) |
| CPI <= 0.30 | 0.25 바로 위 — 첨두 쏠림이 거의 없는 쪽 | q10 = 0.307 근처 (하위 약 10%) |
| WR <= 0.80 | 1.00에서 20% 아래 완충대 | 하위 사분위 **q25 = 0.802** 와 일치 (해당 24.8%) |
| WR >= 1.10 | 1.00에서 10% 위 완충대 | 상위 약 13% (q90 = 1.137) |

**정직하게 적어야 할 한 줄:** 0.40과 0.80이 사분위와 겹친 것은 **사전에 유도한 결과가 아니라 사후에 확인한 일치**다. 설계 단계에서는 "기준선의 1.6배"라는 어림값으로 그었고, 데이터를 본 뒤 그 자리가 마침 q75였음을 확인했다. WR의 0.80/1.10은 1.00 둘레의 **완충대(dead zone)** 이지 그 자체로 의미가 있는 수가 아니다 — 완충대를 두는 이유는 WR이 0.99인 대여소와 1.01인 대여소를 서로 다른 유형으로 부르지 않기 위해서다.

**4) 선을 흔들면 결론이 바뀌는가 — 민감도.**

| 시나리오 | CPI/WR 출퇴근 | 출퇴근형 수 | 순유출 상위 30 중 | 쏠림배수 |
|---|---|---|---|---|
| 채택값 | 0.400 / 0.800 | 144 | 6 | 1.72 |
| 사분위 재보정 | 0.415 / 0.802 | 120 | 5 | 1.72 |
| 귀무기준까지 완화 | 0.250 / 0.800 | 295 | 13 | 1.83 |
| 느슨 | 0.350 / 0.850 | 322 | 10 | 1.29 |
| 엄격 | 0.450 / 0.750 | 63 | 3 | 1.96 |
| 상위 10%만 | 0.463 / 0.656 | 40 | 1 | 1.03 |

*쏠림배수 = (순유출 상위 30개의 출퇴근형 비중) / (전체 출퇴근형 비중). 1.0이면 무작위와 같다.*

여섯 시나리오 전부에서 쏠림배수가 **1.0~2.0 사이**에 머문다. 어느 선을 그어도 "출퇴근형이면 자전거가 부족하다"가 성립하지 않는다. 임계값과 무관한 연속 상관 **CPI - 일평균 순유출 r = -0.062** 도 정의상 시나리오마다 변하지 않는다.

**따라서 이 분석의 결론은 임계값 선택에 의존하지 않는다.** 이용 유형 분류는 부족을 설명하지 못했고(REPORT 7장 「빗나간 가설」), 그 실패는 선을 잘못 그어서가 아니라 **애초에 이용 유형이 부족의 원인이 아니기 때문**이다. 선을 바꿔도 실패가 성공으로 바뀌지 않는다는 것이 이 표의 요점이다.

**5) 남는 한계.** CPI 분포는 단봉(unimodal)이고 뚜렷한 골(valley)이 없다 — 그림 12를 보면 자연스러운 경계가 존재하지 않는다. 즉 대여소는 **유형으로 나뉘는 집단이 아니라 연속체**에 가깝다. 이 경우 어떤 임계값이든 연속체를 임의로 자르는 행위이며, 정직한 대안은 (a) 유형 대신 CPI·WR을 연속 변수로 그대로 쓰거나 (b) 군집화로 경계를 데이터에 맡기는 것이다. 이번 분석은 해석의 편의를 위해 (연속 상관을 별도로 보고하는 조건에서) 임계값 분류를 유지했다.

---

## 4. 데이터 정제 규칙 (기준을 미리 못 박고 시작한다)

| 대상 | 처리 기준 | 이유 |
|---|---|---|
| 관제센터 행 | 별도 플래그 컬럼으로 분리, 대여소 재고 계산에서 가상 노드 처리 | 물리적 대여소가 아니라 관리자 강제 처리 |
| 이용시간 ≤ 1분 **AND** 이용거리 = 0 | 대여 취소로 간주해 제외 (제외 건수와 비율 리포트에 명시) | 실제 이동이 아님 |
| 이용시간 > 24시간(1440분) | 이상치로 분리, 민감도 분석에만 사용 | 분실/미반납 의심 |
| 이용거리 > 40km | 이상치로 분리 (플래그만, 삭제 아님) | 관측 분포의 극단 꼬리. 중앙값 1.0km / 99% 12.4km / 99.99% 25.2km 이므로 40km에는 상위 0.0008%(154건)만 걸린다. 별개로 대전 갑천 자전거길 왕복이 약 40km 로, '하루에 낼 수 있는 최장 거리' 정도가 같은 자리에 떨어진다 |
| 좌표 결측/0 | 위치 현황 CSV로 보완, 안 되면 대여소ID만 사용 | 지도 시각화에서만 문제됨 |
| 반납일시 < 대여일시 | 제외 | 논리 오류 |
| 20개월 사이 신설/폐쇄 대여소 | 관측 시작·종료일 기록, 시계열 비교 시 공통 운영 기간만 사용 | 신설 대여소를 "이용량 급증"으로 오독하지 않기 위해 |
| **축제·행사용 임시 대여소, 초단기 운영 대여소** | `station_type=TEMPORARY` 로 표시하고 **대여소 단위 분석에서 제외** (4-1의 판정 규칙) | 며칠만 존재한 대여소는 순유출·재고 추정에 노이즈만 더한다 |

> **원칙: 어떤 행도 조용히 지우지 않는다.** 제외 규칙마다 제외 건수·비율을 표로 남기고, 주요 결론이 그 규칙에 민감한지 1회 재확인(민감도 체크)한다.

### 4-1. 임시·단기 운영 대여소 판정 규칙

축제·행사용 임시 대여소는 **이름만으로는 구분되지 않는다.** 그래서 관측 가능한 활동 패턴으로 판정한다. 대여소 s의 모든 이벤트(대여+반납)를 기준으로:

| 파생 변수 | 정의 |
|---|---|
| `first_seen` / `last_seen` | 첫 / 마지막 이벤트 날짜 |
| `span_days` | `last_seen - first_seen + 1` (관측 존재 기간) |
| `active_days` | 이벤트가 1건이라도 있었던 날짜 수 |
| `activity_ratio` | `active_days / span_days` (얼마나 꾸준히 살아 있었나) |
| `total_events` | 20개월 총 이벤트 수 |

**판정 로직** — 초안을 실제 데이터에 적용해 보고 **수정했다.** (아래 "왜 고쳤나" 참조)

```
순서대로, 먼저 걸리는 것을 적용한다.

1) CONTROL    : 대여소명에 '관제' 포함                          → 2개. 가상 노드
2) TEMPORARY  : 대여소명에 (축제|행사|임시|팝업|이벤트)
                OR (span_days < 60 AND last_seen < 데이터종료일 - 7일)
3) NEW        : first_seen > 데이터종료일 - 90일                → 신설. 추세 비교에서 제외
4) CLOSED     : last_seen  < 데이터종료일 - 90일                → 폐쇄
5) LOW_USE    : activity_ratio < 0.5 AND total_events < 500
                AND span_days >= 60                             → 저이용 상시 대여소
6) ACTIVE     : 나머지                                          → 부족 진단 본체
```

**왜 고쳤나 — 초안 규칙을 실제로 돌려보니 69개가 TEMPORARY로 잡혔고 대부분 오분류였다**

| 대여소 | 관측 기간 | 이벤트 | 초안 판정 | 실제 |
|---|---|---|---|---|
| `ST1430` 0시축제 임시대여소1 | 13일 | 486 | TEMPORARY | ✅ 맞음 |
| `ST1535` 구암동 구암역 1번출구 건너편 | 21일 (2026-03-11~03-31) | 975 | TEMPORARY | ❌ **신설(NEW)** |
| `ST0380` 둔산동 사학연금회관 | 27일 (2024-08-01~08-27) | 709 | TEMPORARY | ❌ **초기 폐쇄(CLOSED)** |
| `ST1166` 흑석동 흑석네거리 | 607일 | 428 | TEMPORARY | ❌ **저이용 상시(LOW_USE)** |

> 교훈: **"짧게 존재함"과 "임시로 존재함"은 다르다.** 관측 구간이 데이터의 *끝*에 붙어 있으면 신설,
> *앞*에 붙어 있으면 폐쇄, 길게 퍼져 있는데 드물면 저이용이다. 이 셋을 임시와 섞으면
> 멀쩡한 대여소가 부족 진단 대상에서 빠진다.

**처리 원칙**

- 통행 레코드는 **삭제하지 않는다.** 대여소 마스터(`station_master.csv`)에 `station_type` 컬럼으로 표시만 하고, 대여소 단위 분석(순유출 랭킹 · 재고 추정 · 유형 분류)에서 `ACTIVE` 만 필터링한다.
- 전체 이용량 추세(Q1)는 `TEMPORARY` **포함 / 제외 두 버전을 비교**해 결론이 뒤집히지 않는지 1회 확인한다(민감도 체크).
- 제외된 대여소 목록과 사유를 리포트 부록에 표로 남긴다. "몇 개를 왜 뺐는가"는 평가에서 반드시 나온다.
- `CLOSED` 는 부족 진단에서는 빼되, **"왜 폐쇄됐는가"는 별도 관찰거리**다(수요 부족인지 재배치 실패인지).

---

## 5. 처리 파이프라인 (2.3GB를 다루는 현실적인 방법)

노트북에서 20개월치 CSV를 한 번에 `read_csv` 하면 죽는다. **한 번 무겁게, 이후는 가볍게** 전략:

```
[1단계] 원본 CSV (20개월, CP949, 2.3GB)
   │  chunksize + usecols + dtype 지정으로 스트리밍 읽기
   │  월별로 처리 → 정제 규칙 적용
   ▼
[2단계] trips.parquet          (정제된 통행 레코드, 컬럼 최소화)
   │
   ├─▶ [3-a] station_hourly.parquet
   │        대여소 × 시각(1시간) → 대여건수, 반납건수, 순유출
   │        ≈ 1,300개소 × 14,600시간 → int16 저장 시 수백 MB 이내
   │
   └─▶ [3-b] bike_moves.parquet
            자전거별 궤적 + 재배치 플래그
   ▼
[4단계] 분석/시각화는 3-a, 3-b 만 로드 → 노트북에서 즉시 반복 실험 가능
```

권장 도구
- `pandas` + `pyarrow` (기본)
- 대안: `duckdb` 로 CSV/Parquet 직접 SQL 조회 (메모리 부담 최소, 설치 1줄)
- 시각화: `matplotlib` + `seaborn`, 지도는 `folium` 또는 좌표 산점도

재현성 산출물: `requirements.txt` (버전 고정), `README.md`(실행 순서), 원본 데이터는 **저장소에 올리지 않고 다운로드 방법과 출처만 문서화**(용량·라이선스 이슈).

---

## 6. 시계열 분석 기법 (미션 요구: 최소 2가지 → 5가지)

| 기법 | 적용 대상 | 왜 이걸 쓰는지 (평가 시 이렇게 답한다) |
|---|---|---|
| 이동평균 (7일/30일) | 일별 총 대여량 | 요일 주기(7일)를 제거해 **추세**만 보기 위해. 7일 창은 주간 주기의 정확히 한 배수라 요일 효과가 상쇄된다 |
| 변화율 (전월/전년동월 대비) | 월별 집계 | 절대량은 계절에 좌우되므로, **전년 동월 대비**로 봐야 계절성을 통제한 성장 여부를 알 수 있다 |
| 요일 × 시간대 집계 | 대여소별 프로파일 | 부족은 "언제"의 문제다. 일 단위 집계로는 아침 재고 소진이 안 보인다 |
| 누적합(cumsum) 드리프트 | 대여소별 순유출 | 재고 역추적의 핵심. 기울기 = 일평균 재배치 필요량 |
| 시계열 분해 (STL) | 일별 총 대여량 | 추세/계절/잔차 분리. **잔차의 급증 = 이상 이벤트**(기상, 시스템 장애 등) 탐지 |

> **집계 단위 근거 한 줄**(미션 가이드 요구사항): "전체 추세는 일 단위, 부족 진단은 시간 단위로 본다. 부족은 하루 안에서 몇 시간만 발생했다가 재배치로 해소되므로 일 단위 집계에서는 완전히 사라진다."

---

## 7. 시각화 계획 (필수 2개 → 6개)

| # | 그림 | 형식 | 이 그림이 답하는 질문 |
|---|---|---|---|
| 01 | 일별 대여량 + 7/30일 이동평균 | 선그래프 | Q1 추세·계절성 |
| 02 | 시간대 × 요일 히트맵 | 히트맵 | Q2 이용 패턴의 모양 |
| 03 | 대여소별 순유출 Top/Bottom 20 | 수평 막대 (발산형 색상) | Q3 어디가 비고 어디가 넘치는가 |
| 04 | 문제 대여소 3곳의 상대 재고 곡선 (하루 평균 프로파일) | 다중 선그래프 | Q4 언제 비는가 |
| 05 | 순유출 지도 (대여소 좌표 산점, 색=순유출, 크기=이용량) | 지도/산점 | 부족의 공간적 패턴 (지형·권역) |
| 06 | 예측 vs 실제 (홀드아웃 구간) | 선그래프 | Q5 예측 성능과 한계 |

시각화 체크리스트
- [ ] 날짜 축 정렬·누락 구간 확인 — **총 37일 결측**: 2025-02-26~28(3일, 시스템 장애) / 2025-03-01~03(3일, 시스템 장애) / **2025-12 전체(31일, 파일 중복 배포로 데이터 자체가 없음)**. 세 구간 모두 보간하지 말고 선을 끊어서 표시할 것
- [ ] 값 범위가 큰 경우 스케일 점검 (필요 시 로그 스케일)
- [ ] 모든 그림에 제목·축 라벨·단위·출처
- [ ] 한글 폰트 설정 (macOS: `AppleGothic`, 없으면 `NanumGothic` 설치)

> **함정 주의**: 겨울 구간에 결측이 몰려 있다(2월 3일 + 3월 3일 + 12월 31일). 모르고 "겨울에 이용량이 급감했다"고 쓰면 평가에서 바로 지적당한다. **계절 효과와 결측 구간을 반드시 분리해 서술하고**, 월별 비교는 "일평균 대여량"으로 정규화해 결측일 수 차이를 흡수한다.

---

## 8. 보너스 — 예측 (미션 5-2-B)

**예측 대상**: 특정 대여소의 다음 1시간 순유출(또는 상대 재고 변화)

**베이스라인 사다리** (정확도보다 "가정과 한계"를 설명하는 게 목적)

| 단계 | 모델 | 입력 | 원리 한 줄 |
|---|---|---|---|
| **B0** | Naive (지속성 모델) | 168시간 전 값 1개 | "지난주 같은 요일·같은 시각과 같을 것이다" |
| **B1** | Seasonal Mean (계절 평균) | 최근 8주의 (요일 × 시간대) 평균 | 주 단위 반복 패턴만 사용, 노이즈는 평균으로 지움 |
| **B2** | 회귀 모델 (선형회귀 → GBM) | Lag · 롤링 · 순환 인코딩 피처 | 최근 상태 + 시간 구조를 함께 학습 |

**용어 풀이**

- **Lag(시차) 피처** — 예측 시점보다 과거의 값을 그대로 입력으로 넣는 것. `lag_1`은 1시간 전 순유출, `lag_24`는 하루 전 같은 시각, `lag_168`은 일주일 전 같은 요일·같은 시각. 자전거 수요는 "방금 전 상황"과 "지난주 같은 시각"을 **동시에** 닮기 때문에 세 종류를 함께 넣는다.
- **롤링(rolling, 이동창) 피처** — 과거 일정 구간의 요약 통계. `roll_mean_3`은 직전 3시간 평균, `roll_std_24`는 직전 24시간 표준편차(= 최근 변동성). 단일 시점 값보다 노이즈에 덜 흔들려 추세를 안정적으로 전달한다.
- **순환 인코딩(cyclical encoding)** — 시각(0~23)이나 요일(0~6)을 그냥 숫자로 넣으면 모델이 "23시와 0시는 23만큼 멀다"고 오해한다. 실제로는 붙어 있는 시각이다. 그래서 원(circle) 위의 좌표로 바꿔 넣는다.

  ```
  hour_sin = sin(2π × hour / 24),  hour_cos = cos(2π × hour / 24)
  dow_sin  = sin(2π × dow  / 7 ),  dow_cos  = cos(2π × dow  / 7 )
  ```
  이렇게 하면 23시와 0시가 좌표 평면에서 이웃이 되어, 자정을 넘는 연속성이 보존된다.
- **GBM (Gradient Boosting Machine, 그래디언트 부스팅)** — 얕은 결정트리를 하나씩 순차적으로 더해 가되, **각 트리가 직전까지의 예측이 틀린 만큼(잔차)을 맞추도록** 학습하는 방식. 표(tabular) 형태 데이터에서 강하고, "평일이면서 18시이고 직전 1시간 순유출이 컸다면"처럼 조건이 겹치는 비선형 관계를 자동으로 잡아낸다. 구현은 `sklearn.ensemble.HistGradientBoostingRegressor` — 추가 설치 없이 쓸 수 있고 결측치도 그대로 받는다.
- **홀드아웃(hold-out)** — 학습에 쓰지 않고 떼어 둔 검증 구간. 시계열에서는 **반드시 시간 순서를 지켜 맨 마지막 구간을 떼야 한다.** 무작위 분할을 하면 미래 데이터로 과거를 맞히는 꼴이 되어(데이터 누수, data leakage) 성능이 실제보다 좋게 나온다. 이건 시계열 분석에서 가장 흔한 실수이자, 평가에서 좋은 답변 소재다.
- **백테스트(backtest)** — "과거 그 시점에 서서 이 모델을 돌렸다면 얼마나 맞혔을까"를 재현하는 검증. 여기서는 마지막 4주를 홀드아웃으로 두고 1시간 앞 예측을 반복한다.
- **MAE / RMSE** — MAE(평균절대오차)는 오차 크기의 단순 평균으로, 단위가 '대'라서 해석이 직관적이다("평균 2.3대 틀린다"). RMSE(제곱근평균제곱오차)는 큰 오차에 더 큰 벌점을 준다. **재고 소진 같은 큰 실수를 피하는 게 중요하면 RMSE**, 평균적 정확도를 말할 때는 MAE. 둘 다 계산해 **B0 대비 개선율(%)** 로 비교한다.

> **원칙: B0를 못 이기는 모델은 쓸 이유가 없다.** 복잡한 모델을 넣기 전에 B0·B1의 MAE를 먼저 표로 남긴다. "왜 딥러닝을 안 썼나요?"에 대한 답도 이것이다 — 베이스라인을 이기는지 확인하는 게 순서다.

**검증**: 시간 순서 유지 분할(마지막 4주 홀드아웃), 지표 MAE/RMSE, 베이스라인 대비 개선율.

**반드시 리포트에 쓸 한계**
- 날씨(강수·기온) 미반영 — 자전거 수요의 가장 큰 외생 변수
- 재배치 스케줄을 모름 → 재고 예측이 아니라 **"개입이 없을 때의 순유출 예측"**
- 절대 재고 오프셋 미지, 거치대 용량 데이터 없음
- 검열된 수요: 재고 소진 시간대의 실제 수요는 관측 불가 → 예측은 구조적으로 과소추정

---

## 9. 보너스 — 실시간 OpenAPI 모듈 (이력 분석과 **완전 분리**)

> **설계 원칙: 실시간 수집·검증은 이력 분석의 선행 조건이 아니다.**
> 키 발급이 늦어지거나 끝내 안 나와도 본 분석(단계 1~7)은 100% 완결되어야 한다.
> 따라서 별도 서브패키지로 만들고, 두 세계는 **저장 파일 하나로만 만난다.**

### 9-1. 구조 — 독립 서브패키지

```
realtime/                       # 이 폴더는 본 분석 코드를 import 하지 않는다
├── collect.py                  # OpenAPI 폴링 → 적재 (키 필요)
├── schema.md                   # 저장 스키마 정의 (아래 표) — 키 없이 지금 작성 가능
├── storage/                    # tashu_status.sqlite  또는 status_YYYYMMDD.parquet
├── .env.example                # TASHU_API_KEY=...
├── requirements.txt            # 수집기 전용 의존성 (requests 등)
└── README.md                   # 단독 실행 방법 (cron/launchd 등록 포함)
```

### 9-2. 경계면(인터페이스) — 이것만 먼저 못 박으면 나중에 그대로 꽂힌다

수집 저장 스키마

| 컬럼 | 설명 |
|---|---|
| `collected_at` | 수집 시각 (KST, 초 단위) |
| `station_id` | 대여소 ID — **이력 CSV의 `ST####` 와 같은 체계인지 최초 1회 매핑 검증 필요** |
| `bikes_available` | 현재 대여 가능 대수 |
| `racks_total` | 거치대 총 수 (API가 제공하는 경우) |
| `raw_json` | 원본 응답 그대로 (스키마 변경 대비용 보험) |

연결 지점은 **단 하나**: `src/05_validate_with_realtime.py`
- `realtime/storage/*` 를 읽어 `data/processed/` 의 추정 재고와 대조한다.
- 이 스크립트가 **없어도 나머지 파이프라인은 그대로 돌아간다.**
- REPORT.md에서도 본문이 아니라 별도 보너스 절로 분리해 서술한다.

### 9-3. 키 발급 전에 지금 할 수 있는 일

1. `schema.md` 와 `.env.example` 작성 (키 없이 가능)
2. API 응답의 `station_id` 체계가 이력 데이터의 `ST####` 와 일치하는지 확인할 **매핑 검증 체크리스트** 준비
3. 수집은 **키가 나오는 즉시 켠다.** 검증에 최소 2주 축적이 필요하므로, 분석 진도와 무관하게 가장 먼저 가동한다

### 9-4. 수집 2주 후 검증 항목

- **검증 1** — 시간대별 변화 패턴 상관: 추정 Δ재고 vs 실측 Δ재고, 대여소별 상관계수 분포
- **검증 2** — 이력으로 지목한 부족 대여소가 실제로 0대 상태가 잦은가 (재고 소진 시간 비율)
- **검증 3** — 거치대 용량·재고 오프셋을 실측으로 확정 → 추정 재고의 절대값 보정

---

## 10. 산출물 & 폴더 구조

```
tashu-shortage-analysis/
├── data/
│   ├── raw/                     # 원본 CSV (git 제외, .gitignore)
│   └── processed/               # trips.parquet, station_hourly.parquet
├── src/
│   ├── 01_ingest.py             # CSV → 정제 → parquet + 대여소 마스터
│   ├── 02_build_panel.py        # 대여소×시간 패널, 재배치 플래그, station_type 판정
│   ├── 03_analyze.py            # 지표 계산 (CPI/WR, 순유출, 재고 소진 구간)
│   ├── 04_forecast.py           # 베이스라인 사다리 예측
│   └── 05_validate_with_realtime.py   # (선택) 실시간 실측 대조 — 없어도 무방
├── realtime/                    # ★ 독립 모듈 (9장). 본 분석과 파일로만 연결
├── notebooks/
│   └── analysis.ipynb           # 탐색 + 그림 생성 (제출용)
├── images/                      # 01~06 png
├── REPORT.md                    # ★ 제출 본체
├── README.md                    # 실행 방법, 데이터 출처/라이선스
├── requirements.txt
└── PLAN.md                      # 이 문서
```

**REPORT.md 목차** (미션 요구사항 그대로 매핑)
1. 분석 주제 및 선정 이유 (← 개인 경험: 집 앞 대여소에 자전거가 없던 문제)
2. 분석 질문 5개
3. 데이터 설명 (출처·기간·규모·라이선스·알려진 결측 구간)
4. 데이터 정제 (규칙 표 + 제외 건수)
5. 분석 방법 (시계열 기법 5가지와 선택 근거)
6. 결과 및 시각화 (그림 6개 + 각 그림 해설)
7. **인사이트 3개 이상** — 각각 `관찰(Fact) / 해석(Why, 가설) / 행동(Action)` 3단 구조
8. 예측 실험과 한계
9. 결론 및 한계점
10. **AI 사용 로그** (필수)

---

## 11. 인사이트 서술 템플릿 (관찰과 해석의 분리 — 평가 핵심 항목)

각 인사이트는 반드시 이 3층으로 쓴다. 층을 섞으면 감점 포인트다.

```
■ 인사이트 N: (한 줄 제목)

관찰(Fact)  — 숫자와 그래프에서 직접 읽은 것. 추측 없음.
  예) "ST0XXX 대여소는 20개월 누적 순유출이 -N,NNN대이며,
      평일 07~09시 구간에서만 전체 순유출의 XX%가 발생한다. (그림 04)"

해석(Why)  — 왜 그런지에 대한 가설. 반드시 '가설'이라고 명시.
  예) "주거 밀집지에서 출근 시간대에 단방향으로 빠져나가는 패턴으로 보인다.
      반납 상위 대여소가 업무지구·환승역 인근이라는 점이 이 가설과 일치한다.
      단, 통근 목적 여부는 데이터에 없으므로 확정할 수 없다."

행동(Action) — 그래서 뭘 해야 하는가.
  예) "평일 06시 이전 선제 재배치, 또는 해당 대여소 거치 대수 상향.
      다음 실험: OpenAPI 실측으로 06~09시 재고 소진 속도 확인."
```

> 평가자가 가장 자주 던지는 질문: **"그건 관찰인가요, 해석인가요?"** 이 구분을 스스로 말하면서 설명하면 그 자체로 점수다.

---

## 12. AI 사용 로그 (필수 항목 — 미리 기록하며 진행할 것)

작업하면서 아래 표를 **그때그때** 채운다. 마지막에 몰아서 쓰면 반드시 부실해진다.

| 사용 작업 | 사용 이유 | 검증 방법 |
|---|---|---|
| CP949 대용량 CSV 청크 처리 코드 생성 | 반복적인 보일러플레이트 시간 절감 | 1개월 파일로 소규모 재현, 행 수 합계가 `wc -l`과 일치하는지 대조 |
| 재고 역추적 로직 설계 논의 | 대안 접근(순유출만 vs 궤적 기반) 비교 탐색 | 자전거 5대를 손으로 추적해 결과와 대조 |
| 시각화 코드 작성 | 그림 규격 통일 시간 절감 | 축·단위·결측 구간 표시 직접 확인 |
| 리포트 문장 다듬기 | 가독성 | 수치·주장은 원본 집계표와 1:1 재대조 |

**원칙**: AI가 만든 숫자는 숫자가 아니다. 리포트에 들어가는 모든 수치는 본인이 실행한 코드 출력에서 나온 것이어야 하고, 최소 1건은 손계산/샘플링으로 교차 확인한다.

---

## 13. 진행 단계 (권장 순서)

| 단계 | 할 일 | 완료 기준 |
|---|---|---|
| **1. 골격** ✅ | 저장소·폴더 생성, `requirements.txt`, README, `realtime/` 독립 모듈 | **완료** |
| **2. 적재** ✅ | 20개 CSV → 정제 → `trips_YYYYMM.parquet` (932만 행 / 77초) | **완료** → `reports/01_ingest_findings.md` |
| **3. 패널** ✅ | 중복 제거·`station_type`·시간 패널·재배치 탐지 (23초) | **완료** → `reports/02_panel_findings.md` |
| **4. 전체 EDA** ✅ | 그림 01·02·07, Q1·Q2 답 | **완료** → `reports/03_analysis_findings.md` |
| **5. 부족 진단** ✅ | 순유출 랭킹·재고 곡선·지도·유형 분류 → 그림 03·04·05·06 | **완료** (재고 소진 구간 정량화는 5단계로 이월) |
| **6. 예측** ✅ | 베이스라인 사다리 + 백테스트 + 재고 소진 진단 → 그림 08·09·10 | **완료** → `reports/04_forecast_findings.md` |
| **7. 리포트** ✅ | REPORT.md 작성 (인사이트 6개 + 빗나간 가설 1개, 3층 서술, 그림 10개) | **완료** — 수치 자동 검증 23건 전건 일치 |
| **8. 보너스** ✅ | 대시보드(자체 완결 HTML + OSM 지도) · OpenAPI 수집 모듈 | **완료** — 배포 URL: https://builter251.github.io/tashu-shortage-analysis/ |
| **9. 리허설** | 아래 예상 질문에 소리 내어 답해보기 | 막힘 없이 5분 설명 |

각 단계 끝에서 **한 문장으로 "이 단계에서 알게 된 것"** 을 메모해 두면 리포트와 발표가 저절로 만들어진다.

---

## 14. 동료평가 예상 질문 & 답변 요지

미션의 「3. 과제 목표」가 곧 평가 항목이다. 항목별로 준비한다.

**① 데이터 분석 사고**

- *Q. 분석 질문을 어떻게 정의했나요?*
  → 개인 경험(집 앞 대여소 재고 소진)에서 출발해 **현상 파악(Q1·Q2) → 원인 진단(Q3·Q4) → 예측/처방(Q5)** 순으로 계층화했다. 그래프로 바로 확인 가능한 질문과 추가 해석이 필요한 질문을 의도적으로 섞었다.
- *Q. 결측치·이상치를 어떻게 처리했나요?*
  → NaN은 없었다. 대신 **의미상 결측**이 있었다. ① 관제센터 행 2.9%는 물리적 대여소가 아니라 가상 노드로 분리, ② 시간 ≤1분 & 거리 0km는 대여 취소로 간주해 제외, ③ 24시간 초과·40km 초과는 이상치로 분리해 민감도 분석에만 사용, ④ 25년 2~3월 시스템 장애 결측 구간은 보간하지 않고 결측으로 표시. 모든 규칙에 제외 건수와 비율을 남겼다.
- *Q. 관찰과 해석을 구분해 보세요.*
  → (11장 템플릿 그대로 시연)

**② 시계열 데이터 이해**

- *Q. 트렌드/계절성/노이즈를 이 데이터에서 설명해 보세요.*
  → 트렌드 = 20개월에 걸친 이용량의 장기 방향, 계절성 = **두 겹**으로 존재한다(연 단위 여름 성수기/겨울 비수기, 주 단위 요일 효과, 일 단위 출퇴근 이중 피크). 노이즈 = 강수·기온·행사 같은 외생 충격. STL 분해의 잔차가 튀는 날을 실제 사건과 대조해봤다.
- *Q. 이동평균을 왜 7일로 잡았나요?*
  → 요일 효과가 주기 7일이므로, 창 크기를 7의 배수로 잡아야 요일 성분이 상쇄된다. 30일은 월 단위 추세 확인용으로 함께 그렸다.
- *Q. 집계 단위를 왜 시간 단위로 내렸나요?*
  → 부족은 하루 중 특정 시간대에 발생했다가 재배치로 해소된다. 일 단위 집계에서는 이 현상이 평균에 묻혀 완전히 사라진다.

**③ AI 활용 역량**

- *Q. AI에게 뭘 물었고, 어떻게 검증했나요?*
  → (12장 표 그대로) 특히 **재고 역추적 로직**은 AI가 제안한 코드를 그대로 믿지 않고, 자전거 5대의 궤적을 손으로 따라가며 대조했다. 리포트의 모든 수치는 내가 실행한 코드 출력이다.

**④ 예리한 질문 대비 (심화)**

- *Q. 대여가 적은 대여소는 그냥 수요가 없는 것 아닌가요?*
  → 아니다. 관측 대여 = min(수요, 재고)인 **검열된 수요**다. 자전거가 0대면 수요가 있어도 0으로 기록된다. 그래서 대여 건수만으로 수요를 판단하지 않고, 순유출·재고 소진 의심 구간·궤적 단절을 함께 봤다.
- *Q. 재고 절대값을 모르는데 부족이라고 단정할 수 있나요?*
  → 단정하지 않는다. **상대 재고**와 **재배치 필요량**이라는 상대 지표로 순위를 매겼고, 절대값 확정은 OpenAPI 실측으로 넘겼다. 이 한계는 리포트 9장에 명시했다.
- *Q. 이용량이 많은 대여소가 부족한 대여소 아닌가요?*
  → 그렇지 않다. 대여와 반납이 균형을 이루면 이용량이 많아도 비지 않는다. **부족은 총량이 아니라 비대칭의 문제**다. 데이터에서도 대여 상위 대여소와 순유출 상위 대여소가 일치하지 않았다.

---

## 15. 흔한 실패 지점 (미리 피하기)

- ❌ 그래프만 나열하고 해석이 없다 → 미션 제약사항에서 **명시적으로 금지**
- ❌ 25년 2~3월 · **25년 12월** 데이터 공백을 계절 효과로 오독
- ❌ 파일명을 믿고 내용 검증을 건너뜀 (25년12월 파일은 실제로 25년 1월 데이터다)
- ❌ 날짜 파싱 실패를 못 알아챔 — `errors="coerce"` 는 **조용히** NaT를 만든다. 월별 날짜 커버리지 점검을 반드시 넣을 것
- ❌ 대여소를 이름으로 집계 (표기가 월마다 달라 한 대여소가 여러 개로 쪼개진다)
- ❌ 관제센터 행을 일반 대여소로 집계 → 재고 계산이 통째로 틀어짐
- ❌ 20개월치를 한 번에 `read_csv` → 메모리 초과로 시간 낭비
- ❌ 한글 폰트 미설정으로 그래프 라벨이 네모(□)로 출력
- ❌ AI 사용 로그를 마지막에 몰아서 형식적으로 작성
- ❌ 인사이트에 숫자가 없음 ("자전거가 부족하다" ← 근거 수치 필수)

---

## 16. 다음 액션

1. ~~계획 수립~~ ✅ · ~~1단계 골격~~ ✅ · ~~2단계 적재~~ ✅ (`reports/01_ingest_findings.md`)
2. ~~3단계 `02_build_panel.py`~~ ✅ — 중복 제거(2025-12 전량 삭제 확인) · station_type 6분류 ·
   시간 패널 618만 행 · 재배치 89,660건 탐지 · 검증 5종 통과 (`reports/02_panel_findings.md`)
3. ~~4단계 `03_analyze.py`~~ ✅ — 그림 7개 + `station_metrics.csv`.
   인사이트 6개 확보 (`reports/03_analysis_findings.md`)
4. ~~5단계 `04_forecast.py`~~ ✅ — 재고 소진 진단(그림 09·10) + 예측 백테스트(그림 08).
   **직전 시각 재고가 5대 부족하면 다음 시간 대여가 기대치의 82%로 떨어진다**(검열된 수요의 직접 증거).
   예측은 '항상 0' 기준선을 MAE로 못 이긴다 — RMSE로 봐야 한다 (`reports/04_forecast_findings.md`)
5. ~~6단계 `REPORT.md`~~ ✅ — 인사이트 6개 + 빗나간 가설 1개, 그림 10개, AI 사용 로그, 함정 9건 기록.
   `src/verify_report.py` 로 리포트 인용 수치 23건을 산출물에서 재계산해 **전건 일치** 확인
6. ~~보너스~~ ✅
   - 대시보드 완성 및 **GitHub Pages 배포**: https://builter251.github.io/tashu-shortage-analysis/
   - `realtime/` 수집 모듈 완성 — API 명세 확정, 키 없이 셀프테스트 통과
   - `GLOSSARY.md` — 이 프로젝트를 예시로 삼은 용어집 56항목
7. **남은 것**
   - 타슈 OpenAPI 키 발급되면 `realtime/collect.py --loop` 가동 → 14일 축적 후 검증 5종
   - 동료평가 리허설 (14장 예상 질문 소리 내어 답하기)
4. 타슈 OpenAPI 키가 나오면 `realtime/collect.py` 를 **분석 진도와 무관하게 즉시 가동** (검증에 2주 축적 필요).
   특히 **관제센터에 실제 자전거가 거치돼 있는지** 확인하면 3장의 가설 A/B가 판별된다.

---

## 부록 A. 용어 사전 (동료평가 대비 — 내 말로 설명할 수 있어야 하는 것들)

**시계열 기본**

| 용어 | 뜻 | 이 데이터에서는 |
|---|---|---|
| 시계열(time series) | 시간 순서로 기록된 관측값 | 시간별 대여/반납 건수, 추정 재고 |
| 추세(trend) | 장기적인 방향성 | 20개월에 걸친 타슈 이용량의 증감 |
| 계절성(seasonality) | 일정 주기로 반복되는 변동 | **3중**: 연(여름 성수기) · 주(요일) · 일(출퇴근 이중 피크) |
| 노이즈(noise, 잔차) | 추세·계절성으로 설명되지 않는 나머지 | 강수, 한파, 행사, 시스템 장애 |
| 정상성(stationarity) | 평균·분산이 시간에 따라 변하지 않는 성질 | 원본은 비정상. 차분·계절 조정 후에야 모델링이 안정된다 |
| 이동평균(moving average) | 최근 k개 값의 평균으로 곡선을 매끄럽게 | 7일 창 = 요일 효과 상쇄, 30일 창 = 월 추세 |
| 차분(differencing) | 값 대신 "직전 대비 변화량"을 보는 것 | 순유출 자체가 이미 재고의 1차 차분에 해당 |
| STL 분해 | 시계열을 추세·계절·잔차 셋으로 분리하는 기법 | 잔차가 튀는 날 = 이상 이벤트 후보 |
| 자기상관(autocorrelation) | 자기 자신의 과거 값과의 상관 | 24시간·168시간 지점에서 높게 나오면 일/주 주기의 증거 |

**이 분석 고유 개념**

| 용어 | 뜻 |
|---|---|
| **순유출(net outflow)** | 대여소의 (대여 − 반납). 양수면 자전거가 빠져나가는 곳 |
| **상대 재고(relative inventory)** | 순유출의 누적합. 절대 대수는 모르고 **변화만** 아는 재고 곡선 |
| **드리프트(drift)** | 상대 재고가 한 방향으로 계속 흘러가는 현상. 기울기 = 일평균 재배치 필요량 |
| **재배치(rebalancing)** | 운영사가 트럭으로 자전거를 옮기는 것. 자전거 궤적이 끊긴 지점으로 탐지 |
| **검열된 수요(censored demand)** | 관측 대여 = min(실제 수요, 재고). 재고 0이면 수요가 있어도 0으로 기록됨 |
| **재고 소진(stockout)** | 자전거가 0대여서 빌릴 수 없는 상태. 직접 관측 불가 → 대리지표로 추정 |
| **CPI / WR** | 첨두 집중도 / 주말비. 대여소 이용 유형 분류 지표 (3-1) |

**예측 관련** — 8장 "용어 풀이" 참조 (Lag, 롤링, 순환 인코딩, GBM, 홀드아웃, 백테스트, MAE/RMSE)
